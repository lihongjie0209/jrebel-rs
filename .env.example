# JRebel-RS 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# 服务器端口 (默认: 12345)
JREBEL_PORT=8080

# 日志级别 (默认: info)
# 可选值: trace, debug, info, warn, error
JREBEL_LOG_LEVEL=info

# 导出模式 (默认: http)
# 可选值: http, https
JREBEL_EXPORT_SCHEMA=http

# 导出主机 (默认: 空)
# 可以是IP地址或域名
JREBEL_EXPORT_HOST=localhost

# 使用新版索引页面 (默认: true)
# 可选值: true, false
JREBEL_NEW_INDEX=true

# JRebel工作模式 (默认: 0)
# 0: 自动模式
# 1: 强制离线模式
# 2: 强制在线模式
# 3: 旧GUID模式
JREBEL_WORK_MODE=0

# 离线许可证天数 (默认: 30)
JREBEL_OFFLINE_DAYS=30